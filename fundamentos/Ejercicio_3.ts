let estudiantes: string[] = [
    "Juan",
    "María",
    "Carlos",
    "Ana"
];

for (let i = 0; i < estudiantes.length; i++) {
    console.log(`El estudiante ${estudiantes[i]} está en la posición ${i}.`);
}